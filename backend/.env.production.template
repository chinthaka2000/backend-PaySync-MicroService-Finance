# Production Environment Configuration Template
# Copy this file to .env.production and fill in the actual values

# Environment Configuration
NODE_ENV=production
PORT=5000

# Database Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/PaySync?retryWrites=true&w=majority

# Database Connection Pool Settings (Production Optimized)
DB_MAX_POOL_SIZE=20
DB_MIN_POOL_SIZE=10
DB_MAX_IDLE_TIME_MS=30000
DB_SERVER_SELECTION_TIMEOUT_MS=5000
DB_SOCKET_TIMEOUT_MS=45000

# JWT Configuration (Use strong, unique secrets in production)
JWT_SECRET=your_super_secure_jwt_secret_minimum_32_characters_production
JWT_REFRESH_SECRET=your_super_secure_refresh_secret_minimum_32_characters_production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Email Configuration
EMAIL_USER=your_production_email@domain.com
EMAIL_PASS=your_production_email_password_or_app_password

# Security Configuration (Production URLs)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://app.yourdomain.com
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File Upload Configuration
MAX_FILE_SIZE=5242880

# Logging Configuration (Production)
LOG_LEVEL=warn
LOG_FILE_MAX_SIZE=50m
LOG_FILE_MAX_FILES=10

# Redis Configuration (Recommended for production caching)
REDIS_URL=redis://your_redis_host:6379

# Cloudinary Configuration (For file storage)
CLOUDINARY_CLOUD_NAME=your_production_cloud_name
CLOUDINARY_API_KEY=your_production_api_key
CLOUDINARY_API_SECRET=your_production_api_secret

# Additional Production Settings
# SSL_CERT_PATH=/path/to/ssl/certificate.crt
# SSL_KEY_PATH=/path/to/ssl/private.key
# TRUST_PROXY=true
# SESSION_SECRET=your_session_secret_for_production